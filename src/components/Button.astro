---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {
  size?: "sm" | "md" | "lg";
  href?: string;
}

const sizes = {
  sm: "px-2 py-2",
  md: "px-4 py-2",
  lg: "px-6 py-4",
};

const { size = "md", ...props } = Astro.props;
const Component = props.href ? "a" : "button";
---

<Component
  {...props}
  class:list={[
    "inline-block cursor-pointer rounded-full border hover:bg-neutral-100 active:bg-neutral-200 dark:hover:bg-neutral-800 dark:active:bg-neutral-800",
    sizes[size],
    props.class,
  ]}
>
  <slot />
</Component>
